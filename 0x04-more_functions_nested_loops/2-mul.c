#include "main.h"
#include <stdio.h>
/**
* mul - multiply two numbers and print it
* @a: first parameter
* @b: second parameter
* Return: Return 1 when its true
*/
int mul(int a, int b)
{
	a = 4;
	b = 5;
	int prod;

	prod = a * b;
	return (1);
	printf("the product is %d", prod);
}
